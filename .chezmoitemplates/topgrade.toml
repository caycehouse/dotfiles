[misc]
# Just update, don't prompt every step.
assume_yes = true

# Cleanup temporary or old files.
cleanup = true

# Don't automatically upgrade container and helm versions.
disable = ["containers", "helm"]

# Some vim plugins don't play nice.
ignore_failures = ["vim"]

[windows]
# Update winget packages.
enable_winget = true
